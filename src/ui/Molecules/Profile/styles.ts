import styled from 'styled-components';

export const ContainerUI = styled.div`
  display: flex;
  align-items: center;

  > img { 
    margin-left: 1.25rem;
  }

  > div.infos {
    text-align: right;
  }
`;
